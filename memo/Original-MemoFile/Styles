이제 CSS를 적용해 볼건데, 먼저 put기반의 MVP CSS를 사용하고 있던걸 지우자.

base.pug에서 지워주면 되고, font-awesome을 사용할 것이기 때문에 가져오면 된다.

그 다음엔 client 폴더 쪽에서 뭔가 만들어 줘야 한다.

client 폴더 안에 scss폴더 안에 components랑 screens라는 폴더가 있는데, components안에는 headers랑 footers같은 것들을 넣을 것이고, screens 안에는 home이랑 search 같은 것을 넣어줄 것이다.




